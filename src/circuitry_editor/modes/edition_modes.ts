/**
 * Valid edition modes.
 */
export enum EditionMode {
    NONE,               // mode where you can't do anything
    PAN,                // mode where you can pan the canvas
    DRAG_AND_CONNECT,   // mode where you can drag modules around and connect them
}
